const fs = require('fs'); // core module
const cetak = require('./cetak'); // local module
const moment = require('moment'); // third party module

console.log(cetak.cetakNama('Johannes'), cetak.nama, cetak.dataMahasiswa.kelas, `\n`, cetak.dataMahasiswa.cetakMhs());